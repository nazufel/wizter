syntax = "proto3";

option go_package = "/wizard";

package wizard; 

message Wizard {
	int64 id = 1;
	string name = 2;
	string house = 3;
	bool deathEater = 4;
}

service WizardService {
	rpc List(Wizard) returns (Wizard) {}
}